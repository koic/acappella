#!/usr/bin/env ruby

lib_path = File.expand_path('../../lib', __FILE__)
$:.unshift(lib_path)

require 'acappella'

options = ACappella::OptionParser.parse(ARGV)

ACappella::Server.new("druby://#{options[:host]}:#{options[:port]}").start
