#!/usr/bin/env ruby

lib_path = File.expand_path('../../lib', __FILE__)
$:.unshift(lib_path)

require 'acappella'
require 'optparse'

options = ARGV.getopts('u:', 'uri:')

ACappella::Server.new(options['u']).start
